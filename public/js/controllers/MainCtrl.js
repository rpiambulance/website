angular.module('MainCtrl', []).controller('MainCtrl', ['$scope', '$sce', function($scope, $sce) {
    /**
     * Function to trust a string as valid HTML
     * CAUTION: passing a string will override any checks for dangerous or invalid
     *          HTML, use with caution.
     * @param input
     * @returns {*}
     */
}]);